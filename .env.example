# Network Configuration
NETWORK=testnet

# Server Configuration
SERVER_ADDRESS=
PORT=3000

# Client Configuration (for testing only - NEVER commit real private keys!)
# Generate a test keypair by running the client example
CLIENT_PRIVATE_KEY=your_private_key_here

# Optional: Custom API Endpoint
# STACKS_API_ENDPOINT=https://stacks-node-api.testnet.stacks.co

# Optional: Accept Unconfirmed Transactions (for faster testing)
ACCEPT_UNCONFIRMED=true
